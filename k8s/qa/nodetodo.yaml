apiVersion: apps/v1
kind: Deployment
metadata:
  name: todolist
  namespace: qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todolist
  template:
    metadata:
      labels:
        app: todolist
    spec:
      containers:
        - name: nodetodo
          image: todo:1.0
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              value: qa
            - name: mongoDbUrl
              value: mongodb://qa-mongo:27017/todolistDb_qa
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: todolist
  namespace: qa
spec:
  selector:
    app: todolist
  ports:
    - port: 4000
      targetPort: 4000
  type: NodePort
